#include<fstream>
#include<sstream>
#include<string>
#include<iostream>
#include<stdlib.h>
#include<stdio.h>

using namespace std;

class Seq{

public:
	Seq();
	~Seq();
	int method1(int m, int n, int *T);
	void sort(int arr[], int length);
	int update(int max, int m, int arr2[]);

private:
	
};
